--- 
- name: Установка стандартных пакетов
  hosts: all
  roles:
    - role: default_packages
  tags:
    - install_packages

- name: Установка и настройка nginx c кастомной страницей
  hosts: nginx-server # Будет работать на узлах в группе nginx
  roles:
    - role: nginx-server
  tags:
    - nginx-server

- name:
  hosts: nginx-proxy
  roles:
    - role: nginx-proxy
  tags:
    - nginx-proxy
